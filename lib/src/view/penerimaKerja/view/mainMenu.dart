import 'package:flutter/material.dart';

class MainmenuPenerimaKerja extends StatelessWidget {
  const MainmenuPenerimaKerja({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(),
    );
  }
}
